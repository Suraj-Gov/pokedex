import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  listContainer: {
    alignItems: "center",
    display: "flex",
    height: "100%",
    width: "100%",
  },
  buttonContainer: {
    position: "absolute",
    bottom: 30,
  },
});

export default styles;
